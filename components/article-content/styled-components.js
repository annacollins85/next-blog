import styled from 'styled-components'

import { GRID_UNIT } from '../../constants/design'

export const ArticleContentContainer = styled.article`
  font-family: 'Merriweather', serif;
  margin: ${4 * GRID_UNIT}px 0;
`
